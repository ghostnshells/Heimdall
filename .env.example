# Panoptes Server Environment Variables

# Server port (default: 3001)
PORT=3001

# CORS origin for frontend (use your domain in production)
CORS_ORIGIN=https://yourwebsite.com

# NVD API Key (get from https://nvd.nist.gov/developers/request-an-api-key)
NVD_API_KEY=your-nvd-api-key-here

# Admin API key for manual refresh endpoint (set a secure random string)
ADMIN_API_KEY=your-secure-admin-key

# Frontend: Tell the frontend to use the backend API (auto-detected in production builds)
VITE_USE_BACKEND=true

# JWT Secret for authentication (set a secure random string)
JWT_SECRET=your-jwt-secret-here

# Database SSL: rejectUnauthorized defaults to true.
# Set to false only if your database provider uses self-signed certificates.
# DB_SSL_REJECT_UNAUTHORIZED=false

# Environment
NODE_ENV=production
